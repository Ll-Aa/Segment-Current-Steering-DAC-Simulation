#12-bit segmented current steering DAC, with the optimal segmentation scheme obtained through simulation
#binary_DAC.cpp   For a standalone 12-bit binary DAC simulation
#therm_DAC.cpp    Simulation of a standalone 12-bit unit current source DAC
#Segment_DAC.cpp  Simulate the segmented current steering DAC to get the segmentation scheme for binary and unit current sources#Segment_all_DAC.cpp Simulate the segmented current steering DAC to get the final segmentation scheme
